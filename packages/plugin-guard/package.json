{
  "name": "@synergy/plugin-guard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Release-time + runtime freshness guard tooling for the Synergy plugin.",
  "scripts": {
    "check:artifacts": "tsx src/check-artifacts.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "22.10.1",
    "typescript": "5.6.3",
    "vitest": "2.1.5"
  }
}
