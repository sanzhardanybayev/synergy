---
description: Show the Synergy preview server status (running/stopped, pid, URL)
---

Run `node "$CLAUDE_PLUGIN_ROOT/packages/cli/dist/cli.js" preview status` in the current project root and report the output to the user. The CLI prints `running` or `stopped`, the pid (if running), and the URL.
