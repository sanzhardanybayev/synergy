---
description: Initialize Synergy in the current project (creates .synergy/ scaffold)
---

Run `synergy init` in the current project root. Creates `.synergy/sessions/` and a local `.gitignore` that excludes the preview lifecycle files (`preview.pid`, `preview.log`).

Run this once per project before the first `/synergy-spec`.
